#!/bin/bash 

choice=$(printf " Back\n󰏕 Install\n󰏕 AUR Install\n󰏔 Uninstall\n󰏓 Purge\n󰚰 Update" | wofi --dmenu --cache-file=/dev/null -p "")

case "$choice" in
    " Back")
        sh "$HOME"/.bin/mainMenu
        ;;
    "󰏕 Install")
        kitty --title="floatterm" -e sh "$HOME"/.bin/pkgInstall
        ;;
    "󰏕 AUR Install")
        kitty --title="floatterm" -e sh "$HOME"/.bin/pkgInstallAur
        ;;
    "󰏔 Uninstall")
        kitty --title="floatterm" -e sh "$HOME"/.bin/pkgRemove 
        ;;
    "󰏓 Purge")
        kitty --title="floatterm" -e sh "$HOME"/.bin/pkgPurge 
        ;;
    "󰚰 Update")
        kitty --title="floatterm" -e sh "$HOME"/.bin/pkgUpdate
        ;;
    *)
esac
